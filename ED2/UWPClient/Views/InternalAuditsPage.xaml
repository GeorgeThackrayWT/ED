<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:grid="using:Telerik.UI.Xaml.Controls.Grid"
      x:Class="WT.UWP.ED2.Views.InternalAuditsPage"
      mc:Ignorable="d">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <grid:RadDataGrid
                    x:Name="DataGrid"
                    ItemsSource="{x:Bind Path=ViewModel.InternalAudits  }"
                    AutoGenerateColumns="False"

                            >

            <grid:RadDataGrid.Columns>
                <grid:DataGridTextColumn PropertyName="AuditDate"/>
                <grid:DataGridTextColumn PropertyName="Region"/>
                <grid:DataGridTextColumn PropertyName="Manager"/>
                <grid:DataGridTextColumn PropertyName="AuditorFirst"/>
                <grid:DataGridTextColumn PropertyName="AuditorSecond"/>
                <grid:DataGridTextColumn PropertyName="CertifiedCorrectBy"/>
                <grid:DataGridTextColumn PropertyName="CertifiedCorrectDate"/>
                <grid:DataGridTextColumn PropertyName="AutherisedCorrectBy"/>
                <grid:DataGridTextColumn PropertyName="AutherisedCorrectDate"/>
            </grid:RadDataGrid.Columns>
        </grid:RadDataGrid>
    </Grid>
</Page>